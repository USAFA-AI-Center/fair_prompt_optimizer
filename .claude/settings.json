{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=\"$CLAUDE_FILE_PATH\"; if [ -n \"$file\" ] && echo \"$file\" | grep -qE '\\.py$'; then cd /home/ai-user/fair_prompt_optimizer && ruff check --fix \"$file\" 2>&1 | head -20; fi",
            "timeout": 15000
          }
        ]
      }
    ]
  }
}
